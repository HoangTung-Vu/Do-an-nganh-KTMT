# Backend Configuration
database:
  url : "postgresql.c8fu6cocaons.us-east-1.rds.amazonaws.com"

s3:
  endpoint_url: "https://s3.amazonaws.com"  
  access_key_id: ""  
  secret_access_key: ""  
  bucket_name: "cn2ml"  
  region: "us-east-1"  
  use_ssl: true  
  
pdf_processing:
  scale: 2.0 
  conf_threshold: 0.25 
  iou_threshold: 0.50   
  model_path: "./models/yolo-doclaynet.pt" 
  temp_dir: "./temp" 

embedding:
  base_url: "http://localhost:8000/v1" 
  api_key: "EMPTY" 
  model: "BAAI/bge-m3" 
  max_retries: 3
  timeout: 60
  batch_size: 32 

vector_store:
  url: "http://3.87.83.234:6333" 
  api_key: null 
  distance_metric: "cosine" 

indexing:
  chunk_size: 512 
  chunk_overlap: 50 
  auto_index_on_upload: true 

server:
  host: "0.0.0.0"
  port: 8000
  reload: true  

logging:
  level: "INFO"  
  max_bytes: 10485760  
  backup_count: 5
